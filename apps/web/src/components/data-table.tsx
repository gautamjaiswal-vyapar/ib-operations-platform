'use client';import{ColumnDef,flexRender,getCoreRowModel,getFilteredRowModel,useReactTable}from'@tanstack/react-table';import{useState}from'react';export function DataTable<T>({data,columns,isLoading=false,emptyMessage='No records found.'}:{data:T[];columns:ColumnDef<T,any>[];isLoading?:boolean;emptyMessage?:string}){const[filter,setFilter]=useState('');const table=useReactTable({data,columns,state:{globalFilter:filter},onGlobalFilterChange:setFilter,getCoreRowModel:getCoreRowModel(),getFilteredRowModel:getFilteredRowModel()});return <div className="card overflow-hidden p-0"><div className="p-4"><input value={filter} onChange={e=>setFilter(e.target.value)} placeholder="Search records…" aria-label="Search records" disabled={isLoading}/></div><div className="overflow-auto"><table><thead>{table.getHeaderGroups().map(h=><tr key={h.id}>{h.headers.map(x=><th key={x.id}>{flexRender(x.column.columnDef.header,x.getContext())}</th>)}</tr>)}</thead><tbody>{table.getRowModel().rows.map(r=><tr key={r.id}>{r.getVisibleCells().map(c=><td key={c.id}>{flexRender(c.column.columnDef.cell,c.getContext())}</td>)}</tr>)}</tbody></table>{isLoading&&!data.length?<p className="p-8 text-center text-slate-500">Loading records…</p>:!data.length?<p className="p-8 text-center text-slate-500">{emptyMessage}</p>:null}</div></div>}
