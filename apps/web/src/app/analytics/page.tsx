'use client';import{useQuery}from'@tanstack/react-query';import{useAuth}from'@/components/auth/auth-provider';import{api}from'@/lib/api';import{money}from'@/lib/utils';import{Bar,BarChart,CartesianGrid,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';type D={trend:Array<{month:string;target:number;revenue:number;incentive:number}>};export default function Page(){const auth=useAuth();const{data,error}=useQuery({queryKey:['analytics'],enabled:Boolean(auth.session),queryFn:()=>api<D>('/analytics/dashboard')});return <><h2 className="text-2xl font-bold">Analytics service</h2><p className="mb-6 text-slate-500">Historical target, revenue and incentive trends backed by operational snapshots.</p>{error&&<p className="mb-4 text-red-700">{error.message}</p>}<div className="card h-[480px]"><ResponsiveContainer width="100%" height="100%"><BarChart data={data?.trend}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis/><Tooltip formatter={v=>money(Number(v))}/><Legend/><Bar dataKey="target" fill="#94a3b8"/><Bar dataKey="revenue" fill="#2563eb"/><Bar dataKey="incentive" fill="#16a34a"/></BarChart></ResponsiveContainer></div></>}
