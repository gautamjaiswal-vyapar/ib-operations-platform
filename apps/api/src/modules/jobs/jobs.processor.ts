import { Processor,WorkerHost } from '@nestjs/bullmq';import { Job } from 'bullmq';import { ExecutivesService } from '../executives/executives.service';import { PlanningService } from '../planning/planning.service';import { IntegrationsService } from '../integrations/integrations.service';
@Processor('operations')export class JobsProcessor extends WorkerHost{constructor(private executives:ExecutivesService,private planning:PlanningService,private integrations:IntegrationsService){super();}process(job:Job){switch(job.name){case'tenurity.daily':return this.executives.refreshTenurity();case'planning.weekly':return this.planning.createWeekly(new Date(),'system');case'planning.monthly':return this.planning.createMonthly(new Date(),'system');case'bigquery.daily':return this.integrations.syncBigQuery('incremental');default:throw new Error(`Unknown job ${job.name}`);}}}
